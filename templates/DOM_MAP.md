# DOM Architecture Map

> Generated by claude-design-toolkit. Keep this updated after every structural DOM change.
> This is the STRUCTURAL TRUTH of the page -- not design specs, not styling, but what contains what.

## How to Read This Map

- Indentation = DOM nesting (children are indented under parents)
- Warning markers = creates a stacking context or has overflow:hidden (DANGER for z-index/clipping)
- Position and z-index shown for all positioned elements
- Elements that clip children are marked explicitly

## Page Structure

<!-- REPLACE WITH YOUR PROJECT'S ACTUAL DOM TREE -->
<!-- Example: -->

```
.wrapper (relative, width: 390px)
|-- img.layout (pointer-events: none)
|-- #stage (absolute, left:0 right:0 top:0, overflow: HIDDEN [!])
|   |-- .vignette (absolute, inset:0, z:3, pointer-events:none)
|   |-- .character-wrap (absolute, z:1, top:80px, h:340px)
|   |   +-- svg#rig (220px wide)
|   +-- .feed (absolute, z:2, top:420px, bottom:18px)
|-- .handle (absolute, z:20, top:59%)
+-- .panel (absolute, z:25, top:0)
    |-- .panel-bg (absolute, inset:0, white)
    +-- .panel-content (absolute)
```

## Stacking Contexts

| Element | Why it creates a context | Impact |
|---------|--------------------------|--------|
| <!-- e.g. #stage | overflow:hidden | Children cannot render outside this box --> |

## Z-Index Scale

| z-index | Element | Purpose |
|---------|---------|---------|
| <!-- e.g. 1 | .character-wrap | Frankie sits behind everything --> |

## Overflow Boundaries

| Element | overflow | What gets clipped |
|---------|----------|-------------------|
| <!-- e.g. #stage | hidden | Everything inside -- panels, characters, overlays --> |
